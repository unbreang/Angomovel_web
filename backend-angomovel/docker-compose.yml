version: '3.8'

services:

  # ── PostgreSQL ──
  postgres:
    image: postgres:15-alpine
    container_name: angomovel_postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: angomovel_db
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

  # ── Redis ──
  redis:
    image: redis:7-alpine
    container_name: angomovel_redis
    restart: always
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data: